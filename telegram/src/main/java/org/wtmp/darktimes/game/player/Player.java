package org.wtmp.darktimes.game.player;

import org.wtmp.darktimes.game.creature.Creature;
import org.wtmp.darktimes.game.creature.CreatureDefinition;

public class Player extends Creature {
    public Player(int x, int y, CreatureDefinition creatureDefinition) {
        super(x, y, creatureDefinition);
    }
}
