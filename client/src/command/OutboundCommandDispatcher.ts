import {CommandDispatcher} from "./CommandDispatcher";
import {Command} from "./Command";

export class OutboundCommandDispatcher implements CommandDispatcher {
    dispatch(command: Command): void {
    }
}